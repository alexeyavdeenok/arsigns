package com.example.feature_cv.tracker

/**
 * Чистые JUnit-тесты IouSignTracker. Без Android SDK, без эмулятора — секунды на прогон.
 * Потому SignTracker и спроектирован как интерфейс на чистых data-классах:
 * это позволяет проверить логику матчинга/TTL без единой android.* зависимости.
 */
// class IouSignTrackerTest {
//
//     TODO: тест "новая детекция без пары -> создаётся новый трек с новым id"
//     TODO: тест "детекция совпала с треком по IoU -> id сохраняется между кадрами"
//     TODO: тест "трек не нашёл пару N кадров подряд -> удаляется после исчерпания жизней"
//     TODO: тест "трек не нашёл пару 1 кадр, но снова совпал -> не потерян (жизни не истрачены)"
//     TODO: тест "две детекции почти не пересекаются (IoU < threshold) -> не матчатся, оба трека"
// }
